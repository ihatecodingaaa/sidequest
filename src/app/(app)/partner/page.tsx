import { PartnerStudio } from "@/features/partner/partner-studio";

export const metadata = {
  title: "Partner studio",
  description: "How an organisation would author a SIDEQUEST mission.",
};

export default function Page() {
  return <PartnerStudio />;
}
